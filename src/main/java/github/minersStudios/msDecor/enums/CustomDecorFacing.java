package github.minersStudios.msDecor.enums;

/**
 * Place facing enum used for check on which side the decor should be installed, null = all sides
 */
public enum CustomDecorFacing {
    CEILING, FLOOR, WALL
}
